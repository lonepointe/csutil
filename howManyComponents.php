<?php

// how many components needed per given time.
// overall length of time (10 hours default)
// cycle time
// cavities
// still not 100% if we need to know if it's family tool or not.
//  if it's family, one shot would need two boxes.  But there's two cavities.  Yes, because otherwise it would bork the math.

